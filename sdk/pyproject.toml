[project]
name = "promptops"
version = "0.1.0"
description = "Python SDK for PromptOps Dashboard"
authors = [{name = "PromptOps"}]
requires-python = ">=3.9"
dependencies = [
  "requests>=2.32.3"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
